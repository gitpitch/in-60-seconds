version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: echo "Hello, World!"
      - run: pwd
      - run: ls -slrt
      - run: ls -slrt /
